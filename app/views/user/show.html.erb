<div class="row">
  <div class="col-xl-3 col-md-4 col-sm-12">
    <div class="card bg-dark text-center">
      <div class="card-body row justify-content-center align-items-center">
        <div class="col-4 col-sm-4 col-md-12">
          <% if @user.avatar.attached? %>
            <%= image_tag @user.avatar.variant(resize_to_limit: [256, 256]), class: 'rounded-circle profile-avatar w-100' %>
          <% else %>
            <i class="bi-question no-avatar__large card-img-top"></i>
          <% end %>
        </div>

        <div class="col-8 col-sm-6 col-md-12">
          <h4 class="card-title"><%= @user.username %></h4>
          <% unless @user.location.blank? %>
            <h6 class="card-subtitle text-muted mb-2">
              <i class="bi-geo-alt-fill"></i>&nbsp;<%= @user.location %>
            </h6>
          <% end %>

          <% unless @user.bio.blank? %>
            <p class="card-text"><%= @user.bio %></p>
          <% end %>
        </div>
      </div>

      <% if @user == current_user %>
        <div class="card-footer">
          <%= link_to '#', class: 'btn btn-outline-primary rounded-pill', data: { 'bs-toggle': 'modal', 'bs-target': '#editProfileModal' } do %>
            <i class="bi-pencil"></i>&nbsp;Edit
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="col-xl-9 col-md-8 col-sm-12">
    <h1 class="display">Wall</h1>

    <div class="d-flex justify-content-between flex-wrap">
      <%= render partial: 'shared/post', collection: @user.posts %>
    </div>
  </div>

  <%= render 'edit_modal' if @user == current_user %>
</div>